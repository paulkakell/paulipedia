# Changelog

Version format: `xx.xx.xx` as `<Release>.<Feature>.<Fix>`.

## 01.01.00 - 2026-02-24

Type: Additive (non-breaking)

Changes:
- Added a browser-based GUI editor (`editor.html`) to add, delete, rename, and modify items in `data/data.json`.
- Added editor assets (`assets/editor.mjs`, `assets/json_ops.mjs`, `assets/editor.css`).
- Linked the editor from the main page Tools menu.
- Introduced a `VERSION` file for traceable releases.

Why:
- Reduce manual JSON editing and make content updates easier.

Refs:
- GUI-1
